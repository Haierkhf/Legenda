pyTelegramBotAPI==4.9.0  # Для telebot
python-dotenv  # Для работы с .env файлами
requests  # Для HTTP-запросов
fastapi  # Используется в проекте (если точно нужен)
uvicorn  # Для запуска FastAPI (если нужен)
cryptography  # Если CRYPTOBOT_API_KEY требует шифрования
